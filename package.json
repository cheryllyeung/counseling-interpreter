{
  "name": "counseling-interpreter",
  "version": "1.0.0",
  "private": true,
  "description": "Real-time bilingual interpretation system for psychological counseling",
  "scripts": {
    "dev": "pnpm run --parallel dev",
    "dev:backend": "pnpm --filter @counseling-interpreter/backend dev",
    "dev:frontend": "pnpm --filter @counseling-interpreter/frontend dev",
    "build": "pnpm --filter @counseling-interpreter/shared build && pnpm --filter @counseling-interpreter/frontend build && pnpm --filter @counseling-interpreter/backend build",
    "render-build": "pnpm install --prod=false && pnpm build",
    "start": "node apps/backend/dist/index.js",
    "lint": "pnpm run -r lint",
    "typecheck": "pnpm run -r typecheck",
    "clean": "pnpm run -r clean && rm -rf node_modules"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@9.14.4"
}
